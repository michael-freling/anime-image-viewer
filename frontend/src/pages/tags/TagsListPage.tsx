import { FC } from "react";
import TagExplorer from "../../components/TagExplorer";

const TagsListPage: FC = () => {
  return <TagExplorer editable={true} title="Edit tags" />;
};
export default TagsListPage;
